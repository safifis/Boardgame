import backgroundL from "/src/assets/background_l.png";
import backgroundS from "/src/assets/background_s.png";

const Hero = () => {
    return (
        <div className="flex justify-center items-center mt-4">
            <img src={backgroundL} alt="Background Picture" />
        </div>
    );
};

export default Hero;
